#include <iostream>
using namespace std;

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0); cout.tie(0);

    int h, w, n, m;
    cin >> h >> w >> n >> m;

    int cnt = 0;
    cnt += w / (m + 1);
    

    return 0;
}