#include <iostream>
#include <stack>
using namespace std;

stack<int> org;
stack<int> tmp;

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0); cout.tie(0);

    int n; cin >> n;
    for (int i = 0; i < n; i++){
        int x; cin >> x;
        org.push(x);
    }

    int next = 1;
    for (int i = 0; i < n; i++){

    }

    /*

    */

    return 0;
}